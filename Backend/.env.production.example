# Backend Environment Variables for Production Deployment
# ======================================================
# Copy these to Vercel environment variables and fill in your actual values

# Server Configuration
PORT=3000
NODE_ENV=production

# MongoDB Connection
DB_URI=mongodb+srv://username:password@cluster.mongodb.net/gennotes?retryWrites=true&w=majority

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-min-32-chars-recommended
JWT_EXPIRE_IN=7d

# Frontend URL (for password reset links)
FRONTEND_URL=https://gennotes.vercel.app

# CORS - Allowed Origins
ALLOWED_ORIGINS=https://gennotes.vercel.app

# Email Service Configuration (Gmail SMTP)
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_SECURE=false
MAIL_USER=your-email@gmail.com
MAIL_PASS=your-app-specific-password
MAIL_FROM_NAME=GenNotes

# ======================================================
# IMPORTANT INSTRUCTIONS:
# 
# 1. MongoDB Setup:
#    - Create free cluster at https://www.mongodb.com/cloud/atlas
#    - Get connection string and replace in DB_URI
#
# 2. JWT Secret:
#    - Generate random string: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#    - Make it at least 32 characters
#
# 3. Email Setup (Gmail):
#    - Enable 2FA on Gmail account
#    - Generate App Password: https://myaccount.google.com/apppasswords
#    - Use app password in MAIL_PASS (not your regular password)
#
# 4. Vercel Setup:
#    - Go to Vercel Dashboard > Project Settings > Environment Variables
#    - Add all these variables
#    - Re-deploy after adding variables
#
# 5. CORS Origins:
#    - Add your frontend URL (production Vercel URL)
#    - Separate multiple origins with commas
